import React from 'react';

const Navigation = () => {
    return(
        <nav style={{display:'flex', justifyContent: 'flex-end'}}>
            <p className='f3, link dim black underline pointer'>Sign Up</p>
        </nav>
    )
}
export default Navigation;